# 🟢 阶段一：基础与可视化 (Visibility)
**目标**: *看见*集群里发生了什么。你不能保护你看不见的东西。

> **👶 小白概念站**:
> *   **CNI (Cilium)**: 相当于集群的“网卡”和“交警”，负责连接容器并控制流量。
> *   **可观测性 (Hubble)**: 相当于集群的“监控摄像头”，能看到谁在访问谁。

## 📝 学习任务

* [x] **网络层升级 (已完成)**
  * 状态: Flannel -> Cilium 迁移完毕。
* [x] **启用监控眼 (已完成)**
  * 状态: Hubble UI 已部署。
  * **练习**: 打开 Hubble UI (`http://192.168.0.200:8888`)，随意部署一个 Nginx Pod，观察它在 Hubble 中的连线。
* [ ] **熟悉工具链 (K9s)**
  * **练习**: 使用 K9s 进入一个 Pod 的 Shell (`s` 键)，尝试 `ping google.com`。

## 🏆 里程碑练习：星战前传

**任务**: 部署 Cilium 官方 "Star Wars" 演示应用，并在 Hubble 中观察“帝国”与“反抗军”的流量。

1. 部署应用:
   ```bash
   kubectl create -f https://raw.githubusercontent.com/cilium/cilium/HEAD/examples/minikube/http-sw-app.yaml
   ```
2. 观察: 打开 Hubble UI，选择命名空间 `default`。
3. 操作: 尝试模拟一些请求（可以使用 `kubectl exec` 进入 `tiefighter` 访问 `deathstar`）。

**✅ 通关标准**: 在 Hubble UI 中清晰看到 `tiefighter` 访问 `deathstar` 的流量轨迹。

---
*在此目录下创建你的练习笔记或截图...*
